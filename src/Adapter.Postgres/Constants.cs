namespace Adapter.Postgres;

public static class Constants
{
    // Connection string for PostgreSQL database, hardcoded for simplicity
    // In a real-world application, this should be stored in an environment variable
    public const string ConnectionString = "Host=localhost;Port=5432;Database=postgres;Username=postgres;Password=postgres";
}